services:
  app:
    build: .
    container_name: eduquery-app
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TZ: Asia/Jakarta
    ports:
      - "${APP_PORT:-8000}:8000"
    # volumes:
    #   # Uncomment untuk development (hot-reload tanpa rebuild)
    #   - ./app:/app/app
    #   - ./static:/app/static
    #   - ./prompts:/app/prompts